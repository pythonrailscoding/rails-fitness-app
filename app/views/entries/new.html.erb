<% content_for :title do %>
  New Entry &ndash; <%= DateTime.now.to_s(:nice_date) %>
<% end %>

<div class="columns is-centered mt-6">
  <div class="column is-one-third">
  <%= render 'form', entry: @entry %>
    <br><br>
  </div>
</div>
